load_module modules/ngx_http_brotli_filter_module.so;
load_module modules/ngx_http_brotli_static_module.so;
load_module modules/ngx_http_headers_more_filter_module.so;
load_module modules/ngx_http_vhost_traffic_status_module.so;

user angie;
worker_processes 1;
worker_rlimit_nofile 67000;

pid /var/run/angie.pid;

events {
    worker_connections 30000;
    use epoll;
}

http {
    server_tokens off;
    more_set_headers 'Server: Space-Killer-Gateway';
    vhost_traffic_status_zone;

    include snippets/optimization.conf;
    include snippets/gzip.conf;
    include snippets/brotli.conf;
    include snippets/rate_limiting.conf;

    include conf.d/server.conf;

    upstream app_server {
        server app:8000;
        keepalive 64;
    }

    # upstream centrifugo_server {
    #     server centrifugo:8000;
    #     keepalive 64;
    # }

    upstream seaweed_volume {
        server sw_volume_1:8080;
        keepalive          16;
        keepalive_requests 1000;
        keepalive_time     15m;
    }

    include      /etc/angie/mime.types;
    default_type application/octet-stream;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }
}
